{
  "CONSTANTS": {
    "Correct": "Set of correct processes",
    "Faulty": "Set of Byzantine faulty processes",
    "V": "Set of values that may be broadcast"
  },
  "VARIABLES": {
    "sender": {
      "description": "The designated broadcaster process L",
      "init": "sender ∈ (Faulty ∪ {CHOOSE p ∈ Correct : TRUE})"
    },
    "receivedS": {
      "description": "Mapping from each process to the value it has received from the broadcaster",
      "type": "Correct → V ∪ {\"\"} ",
      "init": "If sender ∈ Correct, receivedS maps all correct processes to a chosen value; otherwise unconstrained mapped to V ∪ {\"\"}"
    },
    "delivered": {
      "description": "Delivered value of each correct process, or empty string if undecided",
      "type": "Correct → V ∪ {\"\"} ",
      "init": "All correct processes initially map to empty string"
    },
    "receivedVotes": {
      "description": "receivedVotes[p][v] is the set of processes whose signed vote for v has been received by p",
      "type": "Correct → (V → SUBSET P) ",
      "init": "Initially empty for all p ∈ Correct and v ∈ V"
    }
  },
  "Actions": [
    {
      "name": "SendVote(self)",
      "condition": [
        "self ∈ Correct",
        "receivedS[self] ∈ V",
        "∀ v ∈ V : self ∉ receivedVotes[self][v]"
      ],
      "effect": [
        "∀ p ∈ Correct : receivedVotes[p][receivedS[self]] := receivedVotes[p][receivedS[self]] ∪ {self}"
      ]
    },
    {
      "name": "StepCommit(self)",
      "condition": [
        "self ∈ Correct",
        "delivered[self] = \"\"",
        "∃ v ∈ V : |receivedVotes[self][v]| ≥ N - F"
      ],
      "effect": [
        "Choose such a v",
        "delivered[self] := v",
        "∀ p ∈ Correct : receivedVotes[p][v] := receivedVotes[p][v] ∪ receivedVotes[self][v]"
      ]
    },
    {
      "name": "ByzantineVote(self)",
      "condition": [
        "self ∈ Faulty"
      ],
      "effect": [
        "∃ d ∈ Correct, ∃ v ∈ V : self ∉ receivedVotes[d][v] ∧ receivedVotes[d][v] := receivedVotes[d][v] ∪ {self}"
      ]
    },
    {
      "name": "Next",
      "effect": [
        "(∃ self ∈ Correct : SendVote(self) OR StepCommit(self))",
        "OR",
        "(∃ self ∈ Faulty : ByzantineVote(self))"
      ],
      "description": "Global step relation: some correct process executes one of its enabled actions, or some faulty process acts."
    },
    {
      "name": "Spec",
      "effect": [
        "Init ∧ □[Next]_vars",
        "∀ self ∈ Correct : WF_vars(SendVote(self) OR StepCommit(self))"
      ],
      "description": "Full system spec: initial condition, step relation, and weak fairness for correct-process actions."
    }
  ],
  "Invariants": [
    {
      "name": "TypeInv",
      "description": "All variables remain within their declared types"
    },
    {
      "name": "Consistency",
      "description": "No two correct processes decide different values",
      "spec_instance": "∀ p, q ∈ Correct : (delivered[p] ≠ \"\" ∧ delivered[q] ≠ \"\") ⇒ delivered[p] = delivered[q]"
    }
  ],
  "Properties": [
    {
      "name": "Totality",
      "description": "If some correct process decides, eventually all correct processes decide",
      "spec_instance": "(∃ p ∈ Correct : delivered[p] ≠ \"\") ~> (∀ p ∈ Correct : delivered[p] ≠ \"\")"
    },
    {
      "name": "Validity",
      "description": "If the sender is correct, all correct processes eventually decide its value",
      "spec_instance": "(sender ∈ Correct) ⇒ LET Val == IF sender ∈ Correct THEN receivedS[sender] ELSE \"\" IN ◇(∀ p ∈ Correct : delivered[p] = Val)"
    }
  ],
  "llm_hint": {
    "receivedS": "Models the PROPOSE phase: receivedS[p] is the value that process p received from the broadcaster L",
    "receivedVotes": "receivedVotes[p][v] abstracts the set of processes whose signed vote messages for v have been received by p",
    "Byzantine_model": "Faulty processes may arbitrarily inject vote messages for any value to any correct process",
    "no_message_queue": "Explicit message queues are abstracted away in favor of set-based reception state",
    "commit_forwarding": "StepCommit effect models forwarding of n-f vote messages by adding received votes to all correct processes' receivedVotes for value v"
  }
}